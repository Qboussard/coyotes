<div class="row">
	<div class="col s12">
		<%= form_for(@new, url: admin_article_path(@new.id), html: { method: :put }, id: 'edit_new_{@new.id}') do |f| %>

			<% @new.errors.full_messages.each do |msg| %>
				<%= msg %>
			<% end %>

			<div class="field input-field">
				<%= f.label :title, "Titre" %>
				<%= f.text_field :title, class: 'form-control' , autofocus: true, :class => "validate" %>
			</div>

			<div class="field">
				<%= f.label :summary, "Résumé" %>
				<%= f.text_area :summary, :class => "materialize-textarea"  %>
			</div>

			<div class="field">
				<%= f.label :content, "Contenu" %>
				<%= f.text_area :content, class: 'form-control', :class => "" ,:id => "newContent"%>
			</div>

			<div class="actions btnlog">
				<%= button_tag(type: 'submit', class: "btn") do %>
					Valider <i class='material-icons right'>done</i>
				<% end %>
			</div>
		<% end %>
	</div>
</div>
